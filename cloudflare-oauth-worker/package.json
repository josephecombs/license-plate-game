{
	"name": "cloudflare-oauth-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:run": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"test:ui": "vitest --ui",
		"test:all": "node test/run-tests.js",
		"test:constants": "vitest run test/constants/",
		"test:lib": "vitest run test/lib/",
		"test:routes": "vitest run test/routes/",
		"test:durable-objects": "vitest run test/durable-objects/",
		"test:router": "vitest run test/index.test.js"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"vitest": "~3.0.7",
		"wrangler": "^4.0.0"
	},
	"dependencies": {
		"@aws-sdk/client-ses": "^3.839.0"
	}
}
